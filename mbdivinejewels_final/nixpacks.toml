[phases.setup]
nixPkgs = ["python311Full"]

[phases.install]
cmds = ["python3 -m ensurepip --upgrade", "python3 -m pip install -r backend/requirements.txt"]

[start]
cmd = "python3 -m uvicorn backend.main:app --host 0.0.0.0 --port $PORT"

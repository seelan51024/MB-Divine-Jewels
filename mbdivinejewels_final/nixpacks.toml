[phases.setup]
nixPkgs = ["python311Full", "gcc"]

[phases.install]
cmds = ["python3.11 -m pip install --break-system-packages -r backend/requirements.txt"]

[start]
cmd = "python3.11 -m uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
